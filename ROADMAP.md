# TOTAL MOBILIZATION — Living Roadmap & Model Handoff

**This file is the single source of truth for the ongoing work. Update it EVERY turn and commit it, so nothing is ever forgotten.** It also serves as the send-off / onboarding doc for the next model taking over (the designer is moving the expensive framework builds to **Fable 5**).

**Bank DESIGN DISCUSSIONS and concepts here, not just task lists.** Conversation memory gets compacted; ideas that were discussed but not written here have been lost before. If the designer talks through a concept — even a rejected or someday one — it gets a line in this file.

---

## ⭐ THE DESIGN TENET (read this first, it governs every decision)

> **The game says YES to the player's power fantasy. Difficulty comes from the enemy being overwhelming — NEVER from capping, nerfing, or denying the player their toys.**

The designer's own words: *"you see this thing and say 'oooh I could place 100 of those and it'd be OP?' and then the game says 'errr no.' We say yes."* When something is too strong, the answer is a scarier enemy or a real cost (upkeep/logistics), **not** a hard cap or diminishing returns. Multiplicative aura stacking, 90 airships, a planet-covering army, a wall-of-light particle cannon — all intentional. Let them happen. Fix *perf* and *bugs*, never the fantasy.

---

## 🛠 WORKFLOW & INFRASTRUCTURE (how work actually ships here)

- **The game is ONE file:** `title.html` (~4,700 lines, vanilla JS + Canvas2D, no build step, all art procedural via `ART`/`BART` draw-functions). `index.html` redirects to it. Serve statically, open `title.html`.
- **Delegate the coding.** Implement game changes by dispatching **Sonnet subagents in isolated git worktrees**, not by hand-editing `title.html`. Run parallel agents ONLY on **non-overlapping code regions** (partition by function/region; give each an explicit "do NOT touch" list), then `git merge` each worktree branch back. When two tasks touch the same code, SEQUENCE them. Tell every agent to **commit incrementally** (a session-limit kill with zero commits loses everything — this has happened).
- **The designer's turns are for DESIGN** — ideas, mechanics, feasibility, real opinions (not "nice, big change"). Have opinions. Be a partner. Don't narrate git/merge logistics unless asked.
- **Branch:** develop on `claude/game-balance-tweaks-x4odw9`.
- **Deploy flow (the designer plays `main`):** for each shippable change → bump the footer version + prepend an in-game `<details>` changelog entry (house style: emoji + `<b>` headers) → `git push -u origin claude/game-balance-tweaks-x4odw9` → **deploy:** `git push origin claude/game-balance-tweaks-x4odw9:main`. The designer authorized auto-deploy to main. (`main` may have a stale LOCAL ref — always `git fetch origin main` before reasoning about it.) It's a PWA; a hard refresh busts the cache after deploy.
- **A changelog entry rides with EVERY version.** Standing designer instruction.
- **Verification bar (mandatory, zero pageerrors):** headless boot check before every deploy.
  - Serve: `python3 -m http.server <PORT>` from repo dir.
  - Chromium: `/opt/pw-browsers/chromium-1194/chrome-linux/chrome` args `--no-sandbox --use-gl=swiftshader`.
  - Playwright ESM: import from `/opt/node22/lib/node_modules/playwright/index.mjs`; run via `NODE_PATH=/opt/node22/lib/node_modules /opt/node22/bin/node script.mjs`.
  - In-page: set `localStorage.tm_seenTut='1'` AND `localStorage.tm_seenIntro='1'`. **The intro overlay plays on EVERY load** → after reload, click `.tm-skip` if present, then `[data-act="play"]`, then `waitForFunction(()=>typeof G!=='undefined' && G && mode==='game')`. Assert **zero `pageerror`** (a syntax error stops boot — this is the make-or-break check). Put test scripts in scratchpad, never the repo.

---

## 📍 CURRENT STATE

- **Live version: v3.42.0** (on `main`). v3.40 exotic-jets, v3.40.1 clean vignette, v3.41 fighter-AI-fix + bombardier-nerf, v3.42 SKY FLEET Phase 1. NEXT QUEUED: WW3 nuke easter egg → Aegis smart-rounds → rotor roster → Phase 2 modular mounts (particle-beam apex, NO atmospheric antimatter). DESIGNER PLAYTEST WANTED on the sky fleet (big power spike) + the v3.41 combat fixes.
- (older) **Live version: v3.39.0**: v3.37 escalation+QoL, v3.38 interceptor+aircraft-band+AAR-graphs, v3.38.1 aircraft-fire-up-not-fly-up, v3.39 HELICOPTERS (own arm). Sky-warship CONCEPT ART done (5-ship fleet × 3 languages, all rendered — designer loves all, evolving-hull direction favored, aesthetic not yet locked). Aircraft art glow-up queued (apply chosen ship look to the aircraft roster). NEXT BUILD when designer picks: the capital-ship spine (Sky Dock + Sky Fortress line + modular mounts) in the chosen hull look.
- (older) **v3.36.0**: **v3.35.0 = phased adaptive onboarding** (tutorial). **v3.36.0 = combat perf + balance cuts** (below). **⚑ Super Events engine + Event #1 are CODE-COMPLETE but HELD DORMANT** (`EVENTS_LIVE=false` near the EVENTS array) — the designer wants a full BATCH of events before the decision layer goes public. Flip the flag + restore the "v3.35.0 — THE FIRST DECISION" changelog para to ship it.
- **v3.36.0 shipped (combat/perf + decided cuts):** killed CIWS/rapid-fire per-round impact explosions (the phone-melt perf sink — tracer streak IS the hit now); thinner/smaller tracers; **halved the FREE auto-emplacement rate+damage** (`EMPLACE_DMG_MUL`/`EMPLACE_FIRERATE_MUL` = 0.5 at the EMPLACE block ~L2366 — tune here); Titan now fires `plasarc` siege cannon (dmg 210/rate 3.6/range 720) not a machine gun; CUT `massdrivers`/`voidengine`/`hivemind` nodes + `scout` unit (all leaf/dead, nothing re-pointed); fixed `nanotech` desc to name Nanite Foundry + Turret. NOTE: `scout(x,s,a)` sprite fn (~L1188) is now dead code — harmless, could delete later.
- **v3.35.0 tutorial detail:** HOW TO PLAY reveals systems in phases (industry → doctrine → strikes), **greys out** what isn't relevant yet (Research/Strike tabs dimmed + 🔒 until earned), and adds an adaptive **ADVISOR** corner-coach that nudges on economic imbalance (influence glut→research, no MP→housing/mechanical, IC-rich/inf-poor→units, stability sliding→ease off + Comms/Propaganda) — points a direction, never forces. Skippable + replayable. Tunable `TUNE` block at top of the tutorial IIFE (~line 4980). Directly answers the #1 complaint (too complex to learn).
- **v3.37.0 shipped:** Enemy Escalation v2 (5 map-wide opening bombarders; snowballing unanswered flanks via tunable `ESC` block ~L2610, conservative — DESIGNER PLAYTEST WANTED; universal super-unit groundfall/on-station banners) + QoL batch (dug-in units don't block placement, click-through to occluded buildings, SPEC+INFRA effect listing, factory production readout). `ESC` judgment calls flagged for tuning: opening spread gated to orbital intents; sat-grid suppressed on the 5 opening bombarders (both single-flag reversible if too easy).
- **v3.38.0 shipped:** Anti-Missile Interceptor battery (`interceptor` in BUILD_DEFS — finite 6-round mag, `AMI_REARM=8s`, ceil:0 reaches space, unlocked from start, the dedicated bombardment counter); aircraft ceiling raised one band to thermosphere (`airTopY`→`THERMO_Y`, air ceil default .62→.56, exosphere stays space-only); after-action GRAPHS (`G.hist` time-series sampled ~2s → 4 SVG charts on end screen: economy/stability/forces/fleet); 3 stale-description fixes (heavytank+alloy dropped the cut Heavy Foundry; directedenergy now lists the Warden).
- **In-flight agents:** none. AUTONOMOUS BACKLOG CONVEYOR PAUSED — cleared the entire decided/specced/solo-buildable pile (v3.35→v3.38). What remains needs the designer (see below).
- **REMAINING BACKLOG = needs the designer** (do NOT auto-build these — each is feel-defining or architecture-heavy, wants a design pass WITH the designer, the way events & the tutorial were done): **Morale/Organization Layer-1** (Fable's pick — most novel, everything hooks in), **Garrison v2 supply chain**, **real helicopters**, **units-enter-buildings-as-turrets**, **conventional reinforcement / inherited-nation start / hire-allies influence sink**, **boss→fortress subsystem rework**. PLUS write-blocked: the event batch (Cloning/Superhuman flavor), Capital Fabricator art, lore, music, the ×1/×2.5/×5 difficulty tiers + scenario-equalization + Battleships shields-on-big-units keystone.
- **DESIGNER PLAYTEST WANTED:** escalation v2 feel (v3.37 — tunable via `ESC` block); the new interceptor battery's ammo/reload balance; whether the emplacement nerf went far enough.
- **🎵 THE "SIDE BARS" MYSTERY — SOLVED (was NOT the vignette!):** the persistent black box on the LEFT edge (which the designer kept calling "the vignette/side bars") is the **YouTube music player** (`#ytplayer`, line ~523). It's styled `left:-9999px;1px;opacity:0` — hidden on desktop, BUT **iOS forces YouTube video elements visible** (Apple blocks hidden/1px YouTube playback), so on the designer's iPhone it renders as a black video box top-left. The vignette IS genuinely reduced (1.4%, v3.40.1) — it was never the culprit. Right-edge "bar" = `#achTab` + zoomctl. FIX OPTIONS (designer's call): (a) tuck/clip the player (iOS-stubborn, unverifiable headless — phone test needed); (b) ★ replace YouTube audio with SELF-HOSTED music → removes the iframe entirely, the real cure (ties to the existing music/artist-permissions backlog item). Lesson: don't "fix" by name — verify the actual culprit.
- **🔥 PYRRHIC VICTORY playtest (won a brutal 16-min FULL INVASION — fleet + fixes VALIDATED as winnable-not-trivial):** ⏳ FIXES IN FLIGHT — (1) **Capitulation-crisis recovery ramp**: cities-damaged −45 penalty pinned effStab at 0 → endless "CRISIS/nevermind" flip-flop, comms towers couldn't dig out; fix = crisis becomes a bounded recoverable setback (guaranteed stability ramp back up + drains suspended during recovery + grace cooldown, no oscillation). (2) **Carrier bug** — carriers field NO aircraft in real play (launch path broken); (3) **aircraft STANDOFF** — use range, kite the fortress, don't suicide into its PD; (4) **every capital ship (sky + naval) gets ~2 built-in CIWS turrets** for self-defense on the missile frontline. Designer LOVED the brutal-frontline feel — do NOT reduce difficulty.
- **🔥 LATEST PLAYTEST (lost a FULL INVASION, "felt impossible") — ✅ FIXED v3.41.0:** (a) **Fighter AI was broken** — squads shared one target so all fighters fired dumb non-tracking missiles at a group centroid, didn't diversify, clumped up → fix: independent per-fighter targeting + anti-dogpile spread + missiles that HOME & AIRBURST + looser formation. (b) **Bombardier too strong** — reduce its laser PD, and CLAMP the laser-defense satellites' range (BUG: they snipe across the WHOLE map), general tone-down. (c) OBSERVATION: **carriers are a trap** ("don't make a lot of carriers") — watch carrier value/upkeep. ⏭ QUEUED: **AEGIS BATTERY should fire visibly-distinct SMART/guided interceptor rounds** vs CIWS's dumb `rotary` flak (currently identical — designer asked; never done). ⏭ **more powerful naval + airship units** = the capital-ship build (BLOCKED on the hull-aesthetic pick).
- **NEXT DANGEROUS-TECH EVENTS (designer writing):** Event #2 Cloning (draft 1/2 banked — "How should we treat them": Like humans / Indoctrination-default / …more coming) + Event #3 Superhumans (stub). Don't ship events piecemeal — batch them. Engine + `EVENTS.push` pattern is ready; Fable wires effects when the designer hands over flavor.
- **BACKLOG SWEEP IN PROGRESS** (designer said "FINISH THE BACKLOG"): (a) ✅ CIWS-perf + auto-emplacement nerf + thin tracers SHIPPED v3.36.0. (b) **MORALE/ORGANIZATION** — unified plan banked; Layer-1 spine still to build (feel-critical — build + flag for playtest). (c) Enemy-escalation v2 = IN-FLIGHT now. Then: anti-missile interceptor building, aircraft one-band-up, Garrison v2, helicopters, units→turrets, plus the Small/QoL batch. Lethality = REVERSED, do not build. BLOCKED ON DESIGNER: event writing, Capital Fabricator art, lore, music, the ×1/×2.5/×5 tiers + scenario-equalization + Battleships keystone design calls.
- **HOI4-STYLE POPUP system** (designer idea, ambivalent — "don't bombard the player"): Fable's take = use it ONLY for rare, major beats (a district falls → SHOCK popup; a research combo → society/doctrine DECISION popup), NEVER routine info. The morale shock-jolt is the light version; the full popups belong to the Decision/Society layer. Banked as a rule: one popup per genuinely major event.
- **RESOLVED (emplacement question):** nerf the FREE auto-emplacements only (not built guns, no whiffing); CIWS's per-round explosion spam is the real perf killer. Side-vignette fixed (v3.31.1). Hidden `nightMode` flag awaiting the designer's verdict.
- **Fable 5 has the organizer seat.**

---

## 🚀 THE BIG VISION — "SKY / SEA / SPACE BATTLESHIPS" (the next major build; likely Fable's)

The unifying insight: **the war is VERTICAL. The enemy is UP (orbital siege), not to the side.** So the fantasy isn't ground invasion — it's a **fortress world of capital ships firing upward.** This single reframe fixes the old "ground units feel wonky on one axis" complaint.

- **KEYSTONE — Deflector Shields → auto-shields on every unit bigger than a jet.** Researching Deflector Shields gives all large units (sea battleships, airships, spacecraft, big ground) a shield that **wears down instead of pops.** This *revives the battleship* ("one missile wears the shield" not "one missile kills it"), justifies the airship, and delivers durability all at once. Shields should carry **upkeep / power cost** (a capital investment you field a few of — the ONE place a soft cost is right, and it's a cost, not a cap).
- **Multi-weapon capital ships:** sea battleships, airships (flying AA dreadnoughts — power is compact in the air), and city-sized spacecraft all mount **mixed weapons firing at once** — railguns + lasers + missile cells. Apartment-block gunships with long-range plasma.
- **Weapon-swap upgrades:** planes carry autocannons AND missiles; research swaps autocannon→plasma and missiles→micromissiles per-slot. Reuses the existing `UPGRADES`/`effDef` plasma-re-arm machinery.
- Sea battleships explicitly wanted alongside sky/space — capital ships in all three theaters.

---

## 👾 THE ENEMY OVERHAUL — full design bank (build AFTER 4.0, but the design lives HERE so it's never lost)

**The fantasy:** an invader with orbital supremacy massing a VAST armada you can WATCH assemble in deep orbit during PREP — stationed ships, lit hangars, a countdown — before it descends. Build DREAD. **Phased doctrine:** First Strike (orbital saturation degrades your exposed force) → Suppression (bombardment + jammers ground your air) → Overwhelming simultaneous multi-front landings (burst waves, not a trickle).

- **THE CAPITAL FABRICATOR (the true boss & WIN TARGET):** a dreadnought the WIDTH OF THE SCREEN anchored deep in the exosphere, with **destructible subsystems** — dozens of hangars (endless fighters), AA batteries (PD that downs your aircraft AND rounds), missile launchers. You DISMANTLE it subsystem by subsystem, not chip an HP bar. Deploys **~40 passive laser satellites** the moment the invasion starts. *(Designer is drawing the hull art themselves — transcribe to canvas when it exists.)*
- **Counter-systems** (each hard-counters one dominant player strat; each has a counter-counter): **AA-Missile Fairing "Flak Barge"** (mass-dropped pods that pop into homing-AA batteries — punishes cheap-fighter spam) · **Orbital Laser-Interceptor Grid "Aegis-Net"** (sat grid that auto-lasers your missiles/strikes — punishes missile-only play) · **Orbital Rail-Lance "Skyhook"** (hypervelocity ground strikes, near-uninterceptable — answered by anti-orbital fire/shields/dispersal) · **Air-Superiority Swarm "Reaver wing"** (dogfighters that hunt YOUR fighters) · massed antimatter-slug whistlers + EW jammers.
- **More concepted weapons:** Antimatter Flash Cannon (telegraphed zone-vaporizing ray / kiloton airburst bomblets) · Scalable Plasma (drone-mounted up to building-sized capital plasma mortars) · Antimatter Flak Curtain (airburst WALL across flight lanes) · Smart Rod-Storm (rod saturation aimed at your densest cluster — punishes turtling) · Mag-Tether Grappler (yanks your sats/aircraft UP into a kill zone) · **Adaptive Fabricator** (scouts your composition, builds the counter) · Shutdown Pulse EMP (briefly disables a fraction of electronic units).
- **Flavor concepts (Claude's):** The Eclipse (a ship so vast it casts a moving SHADOW debuff) · Harvesters eat wreckage to fabricate on-site (punishes attrition) · Mimics (late-game copies of YOUR best units) · The Choir (synchronized antimatter emitters, "kill them before the note lands") · The Peeling Sky (temporarily strips your air ceiling).
- **Opening-bombardment arsenal still unbuilt:** nuclear atmospheric EMP · antimatter airbursts (shooting them down still detonates them) · hostile fighter swarms · microrocket drop pods · asteroids (~2500 HP — break them up or catastrophe) · **sun blocker** (deploys if the siege stalls; sits atop the exosphere; tanks stability; space-weapons-only) · centrifugal drone-flinger · constant light bursts · tungsten **cluster-munition buster** variant · flare decoys that actually BREAK missile lock · **relativistic planet-cracker** hard-loss (top intensity tiers only).
- **Bosses → fortresses, not sponges:** lower raw HP, real countermeasures (PD, spawning, hard-to-reach), screen-scale.
- **Scale & perf strategy:** fleet reads as 1000+, but simulate BATTLEGROUP entities (HOI4-style — one entity = a brigade with a count/HP pool, rendered as a cluster). Designer verdict: ~equal numbers + SMART enemies, not literal thousands (phones melt). The **mobilization preview / order-of-battle manifest** ("127 antimatter-slug drones · 339 air-dominance fighters…") sells the true scale in text — it also fixes "the invasion's scale and attack plan was never explained to the player." **Planetary approach view** (cinematic fleet-closing-on-Earth shot) = later dread-polish.
- **Enemy roster consolidation audit** (several ships too similar). **Escalation:** Invasion & Annihilation escalate if you keep surviving with the mothership alive.

## 😰 MORALE / ORGANIZATION — "shock" on the battlefield (designer concept, needs a design pass)
Make stability VISCERAL on the ground. A unit-level **morale/organization** state that mirrors the STABILITY score: when the nation is stable, units fight crisp and organized; when stability craters — because a city block just got leveled and a chunk of the defensive network crumbled — units on the ground **panic and enter SHOCK**, struggling to focus. Designer's framing: *"I'd be freaking the hell out and struggling to focus if I just watched all of Prague get erased off the map."*
- **Driven by stability + shock events:** baseline morale follows `effStab`; a catastrophe (city DISTRICT destroyed, a big defensive structure lost, a nuke nearby) fires a temporary SHOCK SPIKE — a jolt of panic that fades over seconds. Possibly localized (units near the catastrophe shaken hardest) + a global baseline.
- **What shock does:** shaken units fire slower, aim worse (lead/accuracy degrade — they were just given leading; panic undoes it), move hesitantly/scatter, maybe visually tremble. Recovers as stability climbs back and no new shocks land.
- **This is the GRADIENT the designer always wanted:** stability loss should "degrade your units," not instant-loss. The Capitulation Crisis (production freeze + ~30% mutiny at 0 stab) is the rock-bottom extreme; morale is the smooth ramp above it.
- **NO death spiral (critical):** because stability is now cap-based and RECOVERABLE (not a drain), morale recovers with it — a shaken army claws back, it doesn't spiral to zero with no counterplay (the exact thing the designer hated about the old drain). Keep a floor so low morale never = helpless.
- **IMPACT VARIES HEAVILY by unit + situation** (v2 detail):
  - **MECHANICAL units are immune** (drones, androids, mechs, autonomous, machine-family). **Organic/human units falter.** This is the strategic core: a robot army ignores morale entirely (the "machine" society answer).
  - **Faltering behaviors (a spectrum):** hesitate / can't pick a target for a moment · hunker down & get SUPPRESSED · full mental breakdown / lose composure · and at minimum become WASTEFUL — dump their whole magazine in a panic, accidentally over-fire missiles (burns ammo/upkeep, reads as desperation, not just "weaker").
  - **⭐ EVERY RESEARCH PATH is secretly a morale answer** (the insight that makes the tree cohere): Automation → immune robots · Superhumans → fearless demigods · Psionics → mental fortitude/conditioning · Biotech → engineered composure · Propaganda/defensive structures → SUPPRESS panic in a radius. This finally makes the research tree about the game's real question: *how does your civilization keep functioning while watching itself be annihilated?* — the "what does total mobilization turn you into" theme, made mechanical. (Answers the design doc's "societal shift was never represented.")
  - **Shield-watching stress (a PSYCHOLOGICAL input, NOT a shield nerf):** shields stay 100% mechanically identical — same HP/blocking/protection, zero functional downside. The ONLY effect is a MORALE stressor on ORGANIC units sheltering under a shield that's visibly taking hits/wavering (they're objectively safer but rattled watching it weaken). Flows through the normal immunity rules — mechanical units / supermen under a shield don't flinch. You'd still build every shield you can; this is pure dread-flavor on the morale ledger, never a reason to avoid shields.
  - **PRODUCTION shock:** factory/economy workers panic under catastrophe too (9/11 framing — "let me check if my family's okay"), slowing production. The gradient above the Capitulation Crisis's hard production-freeze.
  - **Ties into the DECISION/SOCIETY layer:** dystopian / "mean" doctrine choices interact with morale (a harsher society may suppress panic at a human cost, etc.).
- **UNIFIED PLAN — actual roster susceptibility** (from real `UNIT_DEFS`/`UNIT_FAM`):
  - **IMMUNE (no mind to break):** ALL buildings + emplacement/city guns · drones (`cdrone`, `kdrone`) · robots (`android`, `colossus`) · electronic/orbital platforms (`railgun`, `satlaser`, `missilesat`, `rods`, `spacefrigate`, `ostrikefighter`, `dreadnought`) · nano-constructs (`airship` "Nanofortress", `shifter` nanoswarm).
  - **RESISTANT (engineered/conditioned):** `superhuman` (fearless demigods) · `psitrooper` (psionic fortitude) · `cyborg` (part-machine) · `titan` (part-machine leviathan) · `shock` (gene-conditioned). `clone` = DESIGN CHOICE (eerily-calm bred-numb = resistant + creepy, OR expendable fodder = susceptible — Fable leans resistant/creepy).
  - **SUSCEPTIBLE (ordinary humans — FEEL every catastrophe):** conventional ground (`inf`, `scout`, `tank`, `heavytank`, `aa`, `arty`, `mlrs`, `sam`) · ALL naval crews (`destroyer`→`carrier`) · ALL air crews (`fighter`, `strikejet`, `pdjet`, `bomber`).
  - **⭐ THIS MAPS ONTO THE TREE:** start conventional = a fully susceptible army that feels the war → Automation = immune machine army → Superhumans/Psionics = fearless army → Biotech = conditioned. Your tech path literally decides how much your people feel the horror. That IS the "what does total mobilization turn you into" theme.
- **Fable's design contributions:** (a) high morale could give a small BONUS (crisp/disciplined = +accuracy), so stability is a positive lever not just downside-avoidance; (b) **Propaganda Bureau + Comms Tower should project a morale-STEADYING aura** (suppress panic in radius) — finally gives them a battlefield role beyond the stability cap; (c) dystopia/"mean" doctrine = suppress panic by FEAR (units don't break, but you pay in humanity/stability-cap).
- **BUILD IN LAYERS** (like garrison/stability were): L1 spine = per-organic-unit morale from `effStab` + shock spikes on catastrophe → combat faltering (mechanical immune) + clear feedback (shaken visual + HUD). L2 = research suppression counters. L3 = production shock + shield-stress + society ties.
- **Legibility is make-or-break:** the player MUST see why units falter — shaken-unit visual (tremble/hunker/scatter), a morale HUD readout, a "⚠ SHOCK — [district] LOST" jolt notification.
- OPEN Qs: global-baseline + local-spikes (Fable's vote) vs pure-local? how harsh at the bottom (Fable: sloppy/slow, never frozen)?

## 🎭 SUPER EVENTS (HOI4-style event popups — the delivery mechanism for the Decision/Society layer)
**Designer writes the events; Fable builds the engine.** Fire ONLY on major beats (never spam — see the popup rule). Each event schema:
- `trigger` — a game-state condition (first city lost · research combo completes · stability < X · invasion begins · super-unit groundfall · etc.). Earned, not random.
- `title` + `body` — flavor/voice (the designer's).
- `choices` (optional, 1–3): `{ label, effect }` — effect applies a consequence: ±stability, morale surge/shock, society shift (dystopia/verdant/machine), unlock/lose, doctrine change. Some events are pure DREAD BEATS (no choice, just land + fade).
- `once` (one-shot) vs repeatable.
The best events carry a real CHOICE that shifts what your society BECOMES at a genuine cost (that's the "what does total mobilization turn you into" theme, made interactive). Engine = trigger scan + popup UI (respecting the "major beats only" rule) + effect application. Independent of content — buildable in parallel while the designer writes. **STATUS: ✅ ENGINE + EVENT #1 SHIPPED (v3.35.0, on `main`).** Adding an event = push one `{id,title,body,once,trigger,choices}` object onto `EVENTS` (doc comment in-code shows how). `evtReqMet` gates choices on `{research}`/`{society}`/`{building}`/predicate. Next: the designer writes more choices/events; Fable wires the effects.

### 📜 EVENT #1 — "THE IMMINENT ARRIVAL OF THE AUTOFLEET" (designer-written; seeds the engine)
TRIGGER: late PREP (autofleet inbound, bunker-buster weapons confirmed). BODY (designer's, verbatim in-game): the autofleet is en route with bunker-busters designed to hit civilian housing deep underground; people are fleeing housing blocks into the wilderness, risking abduction & panic (housing blocks keep people fed + in real-time contact with family — losing them causes panic). CHOICES (only unlocked ones show — the tech-gate is the whole point):
- **Reassure them the bunkers are safe** (base): if many housing blocks are later destroyed → MASSIVE stability loss; cap unchanged.
- **Refuse to address it** (base): −~10% stability + reduced influence gain.
- **[Deflector Shields]** personal shield generators in housing blocks: all housing +1 IC/s upkeep, each gains a shield.
- **[Dystopia doctrine]** strict guidelines + prison for leaving + suppress info: lose stability MAX (cap).
- **[Transhumanism]** free data-link bionics (stay updated / call for help): lose a lot of IC, but much less stability/influence loss.
- **[Applied Psionics]** persuade them to stay: lose less cap than dystopia, but still some stability max.
- **[Robotics]** guard drones escort + grid-link the dispersed: like Transhumanism (IC cost, low stability loss).
- (designer will write more tree paths — see the tree→hook map Fable provided.)

### 🎓 TREE → RESPONSE HOOKS for Event #1 (CORRECTED framing — designer set Fable straight)
**THE AXIS (get this right):** this event is *the news breaking that the autofleet is coming and the population starting to panic.* Every choice answers ONE question — **how do you keep a frightened nation from falling apart?** The tech-gate is not flavor: **having a tech is what makes a particular promise BELIEVABLE.** Anyone can say "you're safe"; only a nation that built the shield dome can say it and be believed. Choices that are military actions (contest orbit, strike first) or empty threats do NOT belong here — an empty threat COSTS credibility (designer's point); those are a *different* event (a "wait vs pre-empt" military beat = Event #2 material). Coherent hooks, each a different honest-or-dishonest way to make a scared population believe it'll live:
- **Robotics** → "the machines fight, not your children" — credible only because you have the android army to point at (stab holds, doctrine → machine). *(designer already wrote this one)*
- **Deflector Shields** → "stay under the dome" — true because you built it (real durable calm). *(written)*
- **Dystopia** → arrest the panic-mongers, martial law — order through fear not credibility (stab up now, resentment/cap cost later). *(written)*
- **Nanotech** → "every city is a bunker by morning" — the tech makes an impossible shelter-promise real overnight.
- **Biotech** → steady the population (med/chem morale floor; faintly unsettling it's chemical).
- **Applied Psionics** → broadcast calm — the literal anti-panic tool (fits "every tree secretly manages composure" best). *(written)*
- **Colossus/Titans** → "look at what we built — *they* should be afraid" — confidence through visible overwhelming force (the population can SEE the giant).
- Fold plasma/antimatter/particle into ONE "defiance" option at most, or drop — belligerence reads as fear, not calm.

### 🧬 DANGEROUS-TECH EVENTS (the next vein — designer's idea: dramatize WHY each dangerous tech is dangerous)
**THE HOOK (already half-built):** every `dang:true` research already injects `sc` **unrest** into `stabOffset` on completion — today a SILENT number. Fire a completion event that turns that unrest into a *scene + a fork*: **double down** (full power, full unrest, slide toward a darker society) vs **restrain** (dial the tech back, less unrest, less payoff). This is where dystopia/verdant/machine get CHOSEN, not just tinted. Two chosen as the template (best setting + mechanics already wired): **Cloning** and **Superhumans**.

#### 📜 EVENT #2 (DRAFT) — CLONING / "How should we treat them" (designer-written, 1/2 — MORE COMING; do NOT ship until batch)
TRIGGER: on completing `cloning` research. BODY (designer's, verbatim draft): *"Human cloning. It turns out that unlimited budget, tossing restrictions on national security, state of the art laboratories, and the threat of annihilation can get things done. Despite this, the concerns for such a massive movement are very very real. There was already a major countermovement to human cloning, and they have only intensified as we've finalized our designs. There have been widespread protests at the potential application of these policies and (quite warranted) concerns and discussion around the rights of these clones. We have not made any sort of decision so far as to how exactly we plan to train these clones, the scale of integration or how we want to respond publicly. How should we treat them?"*
CHOICES (draft, 1/2):
- **Like humans.** — less stability loss but slower production.
- **Indoctrination within reason.** We need them prepared; a fundamentally different way to educate them. *(DEFAULT)*
- … (designer writing more)
MECH NOTES (Fable, tunable): the axis is stability/humanity ⇄ production/expendability. "Like humans" = smaller `stabOffset` unrest + a manpower/production throttle; "Indoctrination" = the neutral middle; the coming darker option = cheap/fast clones, bigger unrest, dystopia pull. Ties straight into the existing cloning +60% manpower payoff.

#### ☢ EASTER EGG (designer — DEFINITE BUILD, queued after current agents) — "WORLD WAR THREE / you win by losing"
Launch a NUCLEAR strike during PEACETIME (pre-invasion; naturally-spawning silos exist). Consequence: **instant −90% stability**, EVERY other missile silo salvos **3× nukes at your own cities + military targets**, WW3 begins. Then a joke event popup (designer's copy, verbatim): *"Well, you fired a nuclear bomb. The other powers retaliated through their own stockpile. Congratulations, the autofleet is now diverting course! You've won in stopping the autofleet! You just needed to turn humanity into a crater to do it!"* → a joke "VICTORY" ending (technically you stopped them). VERY BUILDABLE: the peacetime-nuke trigger ALREADY EXISTS (the `oops` achievement detects it), and the SUPER EVENTS engine is already built (dormant) — this popup can hook that. Small, self-contained, on-theme (the dark-comedy thesis of the whole game). Perfect first LIVE use of the events engine.

#### 📜 EVENT #3 (TODO) — SUPERHUMANS / "do the demigods obey you?" (designer to write)
Setting: flying near-immortal laser-demigods that ALREADY carry the game's upkeep (−1 stab cap + 2 influence/s each). Fork: worshipped saviors (verdant, but they answer to no one — bigger unrest/independence) vs a leashed asset (you keep control, they resent it). Mechanics already sitting there (upkeep, cap, verdant society).

## 🗞 THE DECISION & SOCIETY LAYER (the "decision tab" concept — needs its own design pass with the designer)

The original game's biggest missed opportunity, per the designer: **research combos only ever unlocked a unit — they should trigger DECISION CHAINS and NEWS POPUPS with genuinely large impacts on the world and policy.** A decisions/events layer:
- Combo research (Transhumanism, Shapeshifters, Hive Mind territory) presents **choices** with world-scale consequences, announced via news popups — policy shifts, society transformation, not "+1 unit type".
- **Society states must become REAL:** dystopia/verdant/machine are currently just a sky tint + one stability modifier. The societal transformation implied by "total mobilization" should be visible and mechanical (what your civilization becomes).
- **Bioengineering is the designer's chosen axis** for society-level shifts (flagged repeatedly as the interesting tree for this).
- Ties into: Superhuman political costs, dangerous-research unrest, the coup/Capitulation-Crisis framing, and the stability-cap model.

---

## 📋 BACKLOG (prioritized; move to Shipped as done)

### 🔥 COMBAT REWORK — from the latest playtest (a big coherent vision; sequence carefully — shared `hurt()`/projectile hot path)
The designer's combat philosophy: **glass cannons everywhere.** Most units (even large ones) die in 1–2 hits; combined defensive fire instantly deletes enemies (satisfying); **SHIELDS are the ONE survivability layer.** Pieces:
- ~~**Lethality: most units die in 1–2 hits.**~~ **❌ REVERSED by the designer (do NOT build).** Reason (their insight): fast deaths favor the side with MORE guns = the PLAYER, so it makes the game EASIER, not harder — and it kills the prolonged sieges that are the whole point. The real problem was never durability, it was the free auto-emplacement wall (see CIWS/emplacement item). Leave `hurt()` durability AS-IS.
- **PLAYER shields work fine — it's ENEMY shields that are broken.** (Fable misread this.) The player's Shield Generator dome blocks correctly. The fix: make ENEMY shields (beachhead domes / shielded enemy units) BLOCK incoming player fire the SAME WAY the player's shield does. Do NOT rework player shields; do NOT add threshold immunity; do NOT auto-shield big units (that's a later Battleships piece, deferred).
- **The FREE auto-emplacements are the real problem** (RESOLVED framing): the MG nests / flak / field guns that spawn AUTOMATICALLY when infantry dig in are an unstoppable "wall of bullets" that shreds everything — and they're free + absurdly scalable, so they trivialize the game. NERF THESE SPECIFICALLY (fire rate + damage) so a wall is strong-but-beatable and the tankier/saturating enemy can push through. This is NOT a tenet violation — a free auto-win-wall isn't a "toy," it's the game not being a game. **Do NOT make built guns miss/whiff** (whiffing feels broken, not hard) — Fable's counter-proposal accepted by designer's clarification.
- **CIWS perf — the explosion spam is the killer (designer confirmed):** every connecting CIWS round spawns an impact explosion → thousands/sec of pure-cosmetic FX = the lag. Fix: **invisible bullets + a few visible tracers, and KILL the per-round explosion** (sparse spark at most). Also make CIWS read as the anti-ordnance/anti-air SPECIALIST, not the one-true-answer (the lethality change normalizes much of this on its own).
- **Airship → capital ship:** shield + 6+ machine guns + anti-tank guns + MLRS (multi-weapon; Battleships vision).
- **Remove PD lasers from the Fighter** (a fighter carrying anti-missile lasers is a stretch) → make anti-missile lasers a **dedicated aircraft**.

### 🌊 ENEMY ESCALATION v2 — from the lethality-reversal playtest (NEXT enemy build; the *right* way to make it hard)
Instead of nerfing the player, make each landing GROW and blanket the map:
- **Drops ESCALATE:** a drop that starts as ~3 airships can balloon into ~9 airships + a ground landing drop + leviathans, etc. — each landing has the potential to snowball if not answered.
- **Groundfall WARNING banner for ALL super-unit types** (currently only the Land Leviathan gets the "⚠ HAS MADE GROUNDFALL" alert) — give the same warning to bombarders, hives, harvesters, etc.
- **Open the invasion with ~5 bombarders spread across the whole map** (covering every stretch of ground) right AFTER the first missile barrage — so orbital-plasma pressure blankets the ground from the start.
- This is the "difficulty from an overwhelming, escalating enemy" path — the correct alternative to the reversed lethality change.

### 🛰 ENEMY LANDINGS & SUPER-UNIT OVERHAUL — ✅ SHIPPED v3.33.0 (kept here for reference)
- **Every landing includes a super unit; super units come in GROUPS.** Each reinforces if not killed in ~30s (extends existing `BOSS_REINFORCE_T`).
- **Each super unit acts like a CARRIER** — spawns a small group of units.
- **Bombardier kit:** 2× PD fire rate · on arrival deploys **10 railgun sats + 10 laser-defense sats** · arrives with a **support ship that repairs in a radius** · gains a **plasma cannon that snipes GROUND units from space** (oppressive to ground; canNOT snipe air).
- **Bombardment missiles get DURABLE decoys** (take a few hits before breaking apart; current buster decoys are hp:1 — bump them).
- **Ships: fewer drops, ~2× units per drop, targeting the LEAST-defended areas.** Generally units land more in undefended areas and are stronger.

### 🧩 SQUADS — collision rework (designer's revised model; re-touches recent squad work)
Current model = one entity + footprint collision (reads as a blob). Designer wants: **separate collision + separate hitboxes per body, but a shared "brain" (targeting/AI) + a cohesion "pull force"** keeping the bodies clustered. Tightly linked, mostly independent. NOTE: separate collision reintroduces per-body cost — mitigate by keeping the AI/targeting shared (the expensive part stays single) + the existing render LOD.

### 🧵 MULTICORE / WORKERS — Fable's honest verdict: probably NOT worth it
Designer asked to offload targeting math to a worker so the main thread just renders. Reality: Workers can't touch the live object graph — you'd `postMessage`-copy all entity positions out and results back every frame; for thousands of entities that serialization usually costs ~what it saves and adds a frame of targeting latency. Profiling says **RENDERING is the bottleneck, not targeting math** — so the high-value fix is render-side (invisible bullets/tracers, LOD, glow cheapening), not multicore. Revisit only if targeting math ever becomes the profiled hot spot.

### 🚢 CAPITAL SHIPS & THE MODULAR ARSENAL (designer's big convergent vision — THE next major build; design mostly locked below)
The designer talked their way into the whole Battleships keystone + more. It all converges on one system: **a capital-ship / titan tier that your entire tech tree feeds via modular weapon mounts.** Pieces:
- **KEYSTONE — "Miniaturized Shielding"** research (pre: `shieldtech` + `spaceage`). Flavor: the deflector generator shrunk small enough to bolt onto a *moving hull* (that's why it needs Space Age). Does two things: (1) **adds a `shield` pool to your existing big units** (Nanofortress Airship, warships, Colossus/Titan-class ground vehicles); (2) **unlocks the capital-ship tier** (the massive naval battleship). Make it the SHARED ROOT both battleship branches grow from (one "we can build capital ships now" moment).
- **SKY WARSHIP ROSTER (concept art in exploration — 3 design languages being rendered: dirigible / dreadnought / nanotech; designer picks a direction).** These are ~100m armored AIRSHIPS with multiple gun batteries — a CLASS SEPARATE from aircraft (no runways). Roster: **Sky Destroyer** (fast escort/AA, small batteries ~ naval destroyer), **Sky Missile Cruiser** (VLS/standoff ~ naval missile cruiser), **Sky Patrol Ship** (cheap light picket/scout). Weapon loadouts echo their NAVAL counterparts. Each ship has a **weapon PLATFORM that upgrades as you research** (modular mounts — particle/plasma/laser slot in with tech).
- **BUILD FROM A NEW "SKY DOCK" BUILDING** — NOT airbases (airbases are for aircraft; a 100m gun-airship is a different beast). Its own production building.
- **SHIELDS ARE OPTIONAL, NOT REQUIRED** — Miniaturized Shielding is an *upgrade you can choose to add*, not a gate.
- **"Sky Fortresses" research = the unlock UPGRADE-LINE for the big sky ships** (repurpose the existing `airfortress` node), and **the Nanofortress Airship folds into this line as its apex.** Miniaturized Shielding combines in as the optional shield tier. (Research-tree placement finalizes AFTER the art/roster is chosen — design the ships first, shelve them second.)
- **"Superheavy Aircraft"** was the old framing → now folded into the Sky Fortress line above.
- NOTE: these are ROSTER content, NOT tied to the 4.0 milestone (4.0 = the STORY/lore aspect). Roster grows continuously.
- **✅ CONCEPT ART LOVED (all 3 languages).** Designer clarification: **SLEEK IS FULLY APPROVED** — "my problem isn't them LOOKING like a spaceship, it's them BEING a spaceship" (the no-spaceship rule is about game BEHAVIOR/going-to-orbit, NOT visuals). Scale read as "exactly right."
- **LEADING DIRECTION — EVOLVING HULL (designer loves all 3, can't pick):** make the three languages a TECH PROGRESSION — early sky ships look like **dirigibles** → mid like **dreadnought gunboats** → late like **nanotech lifting-bodies** (the modular-mount re-skin system extends to the hull). Turns "which look" into "all of them, as a progression." NOT locked yet, but strongly favored.
- **MORE SHIPS (concepts rendering now, one per language):** **Sky RAILGUN SHIP** (spinal heavy railgun, orbit-reach long-range artillery) + **NANOFORGE CARRIER** (flagship, larger, self-repair, flight deck that launches AIRCRAFT **and HELICOPTERS** — the sky heli-carrier).
- **★ AIRCRAFT ART GLOW-UP (⏳ CONCEPT PASS IN FLIGHT):** redraw the whole PLAYER JET roster (Falcon/Wraith/Solaris/Warthog/Reaper/Warden) prettier — **✅ SHIPPED v3.40.0 — EXOTIC aircraft redraw LIVE.** All 6 player jets (fighter/stealthjet/laserjet/warthog/gunship/pdjet ~L1223-1469) replaced with the approved exotic silhouettes, symmetric wings, player-only (enemy art path `egunship`/`einterceptor`/etc. untouched; tint helpers preserved). NOTE: `gunship`/`stealthjet`/`laserjet` keys are vestigial (no live UNIT_DEF) — redrawn anyway. STILL TO GLOW-UP: naval/ground/drone/enemy art, and the "jets evolve modern→exotic with tech" progression option remains available (era-upgrade reskin) if the designer wants it later. (This also finally kills the long-hated "ugly plane silhouette.")
- **RAILGUN = SMALLER TURRETS, not only the giant spinal gun (designer):** the railgun TANK proves a turret-scale railgun works. Plan: keep the giant SPINAL railgun as the dedicated Railgun SHIP's signature (one big gun = its identity), AND add a railgun TURRET as a weapon MOUNT any multi-turret ship can carry. Both.
- **★ MULTI-TURRETED HEAVY ROSTER (designer):** EVERY naval vessel + heavy airship should be MULTI-WEAPON / multi-turreted like the Land Leviathan (which already fires multiple weapons). This IS the modular-mount capital-ship system applied roster-wide — warships/big-airships carry several turrets, each mount upgradeable by research. Fold into the capital-ship build.
- **⚠ IDENTITY RULE (protects the whole game — designer flagged):** the player ENGAGES up but never OCCUPIES up. You hold surface/sky/sea; the enemy keeps orbital initiative — you're still the besieged defender, just with huge guns. Capital ships FIRE into orbit (like railguns/particle already do; consistent with the v3.38.1 aircraft "fire up, don't fly up" fix) but do NOT live in space. **So 4.0 capital ships = SKY (atmospheric hover-fortress, the airship's apex) + SEA (giant naval super-carrier) ONLY.**
- **SPACE BATTLESHIPS = DEFERRED TO 5.0, behind the ORBITAL RING megaproject.** Do NOT build a space navy in 4.0 — that breaks the defender fantasy. Space combat is EARNED via the ring (the war-flip moment), not default. Sky/Sea now; Space later.
- **★ THE MODULAR MOUNT SYSTEM (the star — fixes "mechanics don't chain", complaint #6):** capital ships (and more units — designer wants modular weapons broadly) have weapon MOUNTS that your OTHER weapon research auto-equips. Research Particle Beams → mounts gain particle cannons; Directed Energy → laser point-defense; plasma/microrockets/etc. slot in. The ship becomes a **living readout of your whole tech tree** — mixing trees finally pays off visibly. IMPLEMENTATION: extend the existing `UPGRADES`/`effDef()` override pattern (already "swaps a unit's weapon when you unlock a tech") into a per-hull `mounts[]` that each weapon-tech toggles on. Enemy super-units are ALREADY multi-weapon (bombarder = PD + plasma cannon + barrage) — same tech, player side.
- **CAPITAL SHIPS ARE CARRIERS — LOCKED.** Graft the working naval-carrier `u.d.carrier` deck-launch (fighters fly out, spend ordnance, land, rearm, relaunch) onto the capital hulls. Flying carrier = the airship's apex form.
- **GIGANTIC-SUPER REWORK:** existing super units get massive bonuses + **gigantic COST** (designer floated ~6× cost / ~2.5× size; a Nanoforge airship the size of a city, dozens of weapons, ~3500 IC — but "expensive enough to justify", tune it). **TENET STEER (important):** gigantic COST is tenet-perfect (game says YES; cost is the balance). A hard "super-unit CAPACITY cap" brushes the tenet — instead use **upkeep + build time + hangar/orbital-lift logistics** as the emergent limiter (a city-sized airship eats a huge chunk of air-command slots + bleeds upkeep → you CAN field a fleet, but the economy screams). Same felt limit, no hard wall, fantasy intact.
- **DECISIONS LOCKED:** (1) capital ships are a NEW TIER ABOVE the airship/carrier, not replacements (you climb to them); (2) Miniaturized Shielding = shared root; (3) they ARE carriers. (4) **✅ HULL AESTHETIC = NANOTECH** (designer picked — sleek anti-grav lifting-body).
- **✅ PHASE 1 SHIPPED v3.42.0 (nanotech):** the 5 sky warships (destroyer/cruiser/patrol/railgun/carrier) as buildable atmospheric capital ships (excluded from air/rotor caps), from a new SKY DOCK building, gated behind the Sky Fortress (`airfortress`) line (Nanofortress airship folds in), carrier launches aircraft+helis, railgun reaches orbit. Fixed loadouts for now. **PHASE 2 (later):** modular weapon-mounts (research reskins weapons onto ships), multi-turret-everything, the optional Miniaturized-Shielding upgrade, and the evolving-hull option (still available). NEEDS PLAYTEST when it ships.
- **★ PHASE 2 MODULAR-MOUNT MAP (designer-approved spec — sky AND naval capital ships):** each ship has mounts (role-fixed + shared hardpoints); weapon research auto-swaps the upgraded module in. SHARED (all ships): Point Defense→flak CIWS · Directed Energy→laser PD + laser secondaries · Miniaturized Shielding→deflector shield · Nanotechnology→nanoforge self-repair · Robotics→armor plating + auto-defense drone turrets. MAIN (by role): base kinetics→railgun turrets · Plasma Weapons→plasma batteries (destroyer/patrol/gunships) · Particle Beams→**particle cannons = the ATMOSPHERIC APEX** (heavy mounts + railgun-ship spinal) · Microrockets→splitting micro-rocket VLS (cruiser). **⚠ NO ANTIMATTER on atmospheric ships (designer physics correction):** a direct-fire antimatter beam/projectile annihilates with AIR at the muzzle → suicide weapon; that's WHY the Antimatter Dreadnought is space-only. So **antimatter direct-fire = SPACE-EXCLUSIVE** (Dreadnought now; the space battleships unlocked by the Orbital Ring in 5.0). This makes antimatter the concrete REWARD for reaching orbit — atmospheric fleets top out at particle beams; you must LEAVE the sky to wield antimatter. (Lore-legal exception: a CONTAINED antimatter MISSILE warhead — annihilates only on impact — is fine through atmosphere, exactly how the Antimatter Strike already works; only the beam/gun can't exist in air.) EXOTIC: Automation→carrier drone bay · Telekinesis→tractor-beam mount · Applied Psionics→psi-disruptor. Goal = a fully-teched Nanoforge Carrier is a living readout of your whole tree.
- **TITAN / new hulls:** superheavy tank; **hover tank past Fusion Age** (BUILD EARLY, cheap+huge payoff — fixes the oldest ground complaint: tracked units become FASTER and AMPHIBIOUS, driving over water like solid ground; clean UPGRADE-table swap).

### 🚀 SUPER UNITS BEYOND THE TREE (designer has "tons" — exotic acquisitions, not normal research)
- **PROJECT ORION** (designer) — nuclear-pulse-propulsion behemoth (rides a wave of nuclear detonations) aimed at the orbital capital ship; on approach it **FRAGMENTS INTO A SWARM OF INTERCEPTOR MISSILES**. Reuses the MIRV/micromissile split-munition code. A boss-killer superweapon with spectacle. Acquire outside the normal tree (decision tree / special unlock).
- Designer wants a whole class of these exotic supers acquired via decision trees / events rather than the research board. Ties to the SUPER EVENTS engine (already built, dormant) + the dangerous-tech events vein.

### 🌌 SANDBOX / "TOTAL WAR" MODE (the north star — justifies every gigantic toy above)
A fully-researched nation (all tech unlocked) vs escalating / themed enemies — including **fictional-universe aliens** — to feel out "how much could we actually fight off." Low-ish effort: an all-unlocked scenario + a tunable/escalating enemy, riding the existing `SCEN` custom-scenario editor. Build it RIGHT AFTER capital ships so there's an arena to bring them to. This is the mode the whole arsenal exists for.

### 🎮 "PLAY AS THE AUTOFLEET" + TWO-PLANE MULTIPLAYER (designer idea — great fantasy, honest costs)
Designer: "one player playing the autofleet would be super fun!" — and it's right (asymmetric besieger-vs-besieged is proven fun; the game already has a rich decision-making attacker AI to inherit). **Fable's honest cost read (why 2P is a 2.0/sequel-scale bet, NOT near-term):** (1) **NETCODE is the monster** — this is a single-file, client-side, zero-network game; real-time 1v1 needs state sync / relay-or-P2P / latency handling = arguably a bigger lift than the whole rest of the roadmap combined. (2) **Attacker has nothing to do during PREP** → you'd have to design+balance a whole SECOND (attacker) economy/build-up game. (3) **Asymmetric balance is a forever-job** vs a human who exploits/focus-fires/times pushes; SP tuning won't transfer.
- **★★ THE ACTUAL BUILD PATH (designer's refinement — solves the defender-AI problem entirely): SIEGE A SAVED DEFENSE.** No 2P, no AI. The defender is a **SAVED SNAPSHOT of a real base** — and a base already runs its OWN guns (emplacements/CIWS/garrison/interceptors/shields all auto-fire), so no defender AI is needed at all: it's a self-defending fortress. You show up as the autofleet with a **FIXED STOCKPILE of strikes + deployment drops** (a loadout budget) and try to crack it. It's a PUZZLE / SCORE-ATTACK ("here's a fortress + 12 bombs + 4 drop pods, break it"), not a balance nightmare. Win = level the cities / a %-destroyed + time score.
  - **SNEAKY UNLOCK — async social w/o netcode:** because a defense is just a SAVED FILE, you can attack SOMEONE ELSE'S saved base (share fortress → friend assaults → compare scores). ~80% of the 2P social juice, zero networking.
  - **THE ONE DEPENDENCY: a base SAVE/SERIALIZE system** (game is currently session-only). Snapshot `G.buildings`/units/terrain/garrison → JSON → restore into a fresh battle. Bounded work, and it gives normal save/load as a bonus. Attacker toolkit = reuse the existing STRAT/strike system. THIS is the path (supersedes the earlier "SP vs defender-AI" idea).

### 🏛 MEGAPROJECTS (5.0 — designer wants each as its own thing)
Wonder-scale endeavours, each a distinct build (not just a research node): the "trump cards" that push the faction UP a power bracket. Examples in flight from the powerscaling chats: **Project Orion** (nuclear-pulse boss-killer that fragments into an interceptor swarm), **relativistic kill vehicle** (antimatter slug flung from the system's edge at ~0.9c — surface-scouring/extinction-tier, a genuine planet-ender), literal planet-crackers (require absurd energies — a megaproject, never a strike button). Megaprojects are the 5.0 headline.
- **★ ORBITAL RING (the 5.0 centerpiece — designer's, and it's great):** a colossal megaproject that pushes the exosphere ceiling WAY up (~200% taller space band), unlocks a whole new tier of **space-scale construction**, and — crucially — is **THE gate that lets you finally build/field in ORBIT.** This is the moment the war FLIPS from defense to offense. Canon justification: capital ships of true space scale are "too big to assemble on Earth" → you need the ring. Fighting the autofleet IN SPACE lives behind this. Pairs with the TM2 counter-offensive arc.

### 📖 LORE / POWER-SCALING ANCHOR (worldbuilding — seeds the lore layer + sandbox enemy tiers)
Established in-conversation, keep consistent: **the faction is a single fully-mobilized planet/nation ≈ upper-0.9-to-1.1 Kardashev-I** — antimatter (extractors → mass antimatter), orbital kinetic bombardment, particle weapons, psionics (Mind-Forge), superhumans, nanotech, self-repair — but **sublight, defensive, cannot leave orbit**. The ultimate PLANETARY LAST STAND; can gut anything sublight, breaks against FTL empires that park in high orbit and Exterminatus. **Key scaling levers:** (1) capital ships are ~5 km (bigger than a standard 1.6 km ISD — we out-mass most fictional *line* ships; only top-1% outliers like SSDs/Death Stars/Hive Fleets/Necrons/committed Crusades are "nope"); (2) **telekinesis denies orbital supremacy** — both sides can tractor capital ships OUT of orbit, so "win orbit then glass" (most invaders' whole plan) fails against us; (3) the **autofleet itself is an S-tier (~Covenant-tier) combined-arms antagonist** with RKKVs — beating it is the measuring stick. (4) **ENGAGEMENT-ENVELOPE EDGE (the sharpest lever):** our entire tree reaches ground-to-orbit (`ceil:0` railguns/particle/missiles/orbital strikes) while most fictional fleets (esp. Star Wars) fight at close/visual range behind shields. We pick the range and deny theirs — enemies fight in OUR band (sustained antimatter+particle+kinetic + tractor pull) or not at all. Plus **shield-attrition**: continuous multi-vector annihilation is a damage curve turbolaser-tuned shields aren't built for, and **nanofabrication rebuilds emplacements in seconds** → we out-TEMPO attrition foes and win the long war by default. Useful for: the sandbox "fictional aliens" roster tiers and any future lore/story layer.

### 🌐 SETTING CANON — THE WARP CATAPULT (designer lore, keep consistent; seeds story layer + TM2)
- The aliens who sent the autofleet used a **WARP CATAPULT** to FLING the ships here — this is WHY the autofleet has no native FTL (one-way launch). It's the single fact that keeps them beatable.
- **ONE catapulted fleet, not endless launches** (designer tightening): the catapult flung a SINGLE autofleet that then spent YEARS massing in orbit to build the threat — the **PREP countdown IS that buildup finishing**, then it descends in waves. A contained, FINITE force → "destroy the fleet" stays a real win condition (which the game already treats it as). NOT infinite FTL reinforcement.
- **Endgame / sequel arc ("TM2"):** "it's only a matter of time before we figure it out" — humanity reverse-engineers the catapult, gains FTL, and the campaign FLIPS: survive the siege → colonize the solar system (space elevators + orbital rail already exist) → eventually shoot BACK down the catapult at whoever sent it. Post-siege colonization/counter-offensive is a whole future mode.
- **Plasma-signature capital hull:** fold into the CAPITAL SHIPS build — a distinct plasma-leaning battleship (the modular mounts make it trivial) next to the antimatter and kinetic hulls. Designer wants a "wall of superheated death" ship.
- **The death-gun stays (canon-justified):** the aura/ammo-box network turning a particle beam / railcannon into an "880mm supersonic sabot machine gun" is gloriously silly — KEEP it (designer's call), lore = "we solved the logistics so the gun never stops." (Matches the existing "don't nerf the super mega death gun yet" note.)

### NEXT BIG BUILDS
1. **Sky/Sea/Space Battleships** → now the CAPITAL SHIPS system above (design locked; spine of the next major build).
2. **Garrison v2 — supply chain (emergent cap, no hard wall):** upkeep-heavy **Factories** → nearest **Depots** → nearest **buildings** (proximity logistics). Remove per-unit upkeep (upkeep lives on factories). Limits are economic + logistical only. (Current garrison is v1: passive per-building auto-fill + per-unit upkeep + a hard global Stockpile weight cap.)
3. **Real helicopters** (designed long ago, NEVER built — biggest miss; designer LOVES these, strong "do next" candidate — self-contained). Full spec (designer): **garrison basically everything** (no runways needed) as CLOSE AIR SUPPORT; slow/cheap/close-range, **low ceiling** (tropo-only, can't reach high enemies — keeps them out of air-spam), land in cities/depots to rearm. **SEPARATE from Air Command** — their own capacity pool (a "$60M heli vs a $300M jet are different logistics"); own build category. **Different combat AI** than jets (hover, hug terrain, loiter CAS — not the fast fixed-wing RTB model). **Helicopter-depot buildings + infra upgrades** that let buildings garrison &/or refuel helis, and turn city tiles into heli garrisons/swarms. Sensible defaults if built: new "Rotor Command" capacity filled by helipads/heli-depots; helis garrison into buildings & cities; low-alt CAS AI. NOTE: roster content, not milestone-gated. **✅ SHIPPED v3.39.0:** ROTOR COMMAND pool (`rotorCap`/`rotorUsed`, separate from Air Command; cities give baseline + `helidepot` building +6), no-runway rearm at cities/depots/carriers (`nearestHeliBase`), low-alt HOVER CAS AI (`d.rotor` branch — closes, hovers, drifts with target; tropo ceil .78), Attack Heli "Kite" (rotor unit) + Microrocket Kite upgrade, and naval Carrier launches a gunship heli (`heliCarrier` hook for the future sky Nanoforge Carrier). TUNABLE: `ROTOR_SLOTS`/`CITY_ROTOR`; heli-depot uses `prod:'air'` so it can also roll jets (easy to restrict).
- **⏭ ROTOR ROSTER EXPANSION (designer wants — currently only the Attack Heli):** add more rotor units — a helicopter with **short-range air-to-air missiles** (heli-vs-air interceptor), and **large rotor-based vehicles** (heavy transport/gunship rotorcraft, tiltrotors). Real helicopters aren't just one CAS gunship. Queue as content.
4. **Aerowork-style garrison buildings:** buildings that grant garrison capacity / unit-upgrades / new unit-types to *neighboring* buildings; "+1 missile capacity" buildings; **helicopter-depot** buildings that give city tiles a helicopter garrison → turn a city into a helicopter swarm.
5. **Units enter buildings → become turrets** (huge perf + cinematic win; ties to garrison): a stack cap, click a building to see who's inside, building-type buffs.
6. **Apocalypse as honest ENDURANCE mode:** replace the demoralizing "survived 9%" with a clear survival objective (e.g. HOLD FOR X) + a personal best to beat. General & below stay winnable; Apocalypse is a last-stand you chase a record on.

### MEDIUM / FEATURES
7. **Anti-missile interceptor building** — laser-tower-sized, ~1500 IC / 100 MP / 30 influence, 4 missiles, reaches space, slow reload, buriable in a silo. The dedicated bombardment counter. (Bombardment code now merged — UNBLOCKED.)
8. **Aircraft reach one band up (thermosphere)** — partial walk-back of the hard mesosphere cap; exosphere stays space-only. (UNBLOCKED now.)
9. **Multi-weapon big units** (titans, land leviathans, warships get secondaries) — largely folded into Battleships.
10. **Lasers → damage-over-time** (a beam melts, it doesn't "hit"). Shifts lasers to the "melt the big slow thing" role; flak handles swarms.
11. **Conventional reinforcement — "you're not alone":** if the game detects the player is woefully underprepared, allied conventional forces surge in (≈4 carrier strike groups, 4 armored battalions, city defenses, underground barracks); some aid even if prepared. Cities also self-improve (bunkers, heli bases).
12. **Inherited-nation start:** cities begin with a few garrison stockpiles/factories + 2–3 pre-placed missile silos, so you inherit a country instead of an empty map.
13. **Influence "hire allies" sink:** buy a Carrier Strike Group for ~200 influence + IC + MP, like a strike, especially pre-invasion. Late-game influence sink.
14. **Naval identity:** differentiate Missile Cruiser (AA/point-missile escort) from Arsenal Ship "Leviathan" (VLS saturation); untangle the name clash with the (now-removed) Arsenal Ship building. Distinct warship hulls already shipped.
15. ~~**Achievements + coup screen**~~ ✅ SHIPPED v3.32.0 — coup defeat reframe + 5 medals (`couped` "Regime Change", `nukehappy`, `oops`, `forgotresearch`, `pacifist` "Who Needs Science?" = win with zero research) + unlock toasts.

### SMALL / QoL (new from latest playtest)
23. **MAP-GEN: too much water + gigacity clumping (designer playtest, mild).** Some seeds roll almost entirely SEA ("Waterworld") with only a few dry spots, and development piles into ONE gigacity because there's no land to spread onto. Keep map VARIETY (designer likes varied maps) but tune the terrain generator: guarantee a MINIMUM usable coastline/land fraction, and spread city seeds apart so you don't get one mega-blob in an ocean. Low-effort generation tuning; not urgent ("it's fine to be honest").
17. **Fortified/dug-in units should NOT block building placement** (currently you can't build where units have fortified).
18. **Click-through occluded buildings:** if a unit is in front of a building, clicking should let you reach/see the building behind it (selection cycling / building priority in `pickAt`).
19. **End-screen post-game graphs** ("the 9000 graphs") — rich after-action charts.
20. **Special-building effects listed in BOTH the SPEC menu AND the Infrastructure menu** (currently only one).
21. **Factory production readout:** show what garrison factories are producing, how much, how fast.
22. **Descriptions audit (partly done):** stale unit/research text. CONFIRMED BUG: the **Nanotech** research node only advertises the Autonomous Plant — it does NOT mention the **Nanite Foundry** and **Nanite Turret** it also unlocks (both `req: 'nanotech'`). Fix the node's blds list/description.

### AUDIT — DECIDED CUTS / FIXES
- **CUT:** Mass Drivers (pure +35% dmg stat), Scout "Lynx" (recons nothing — no fog of war), Hive Mind (never integrates cleanly). **CUT or fold power into something real:** Void Engine (pure +80% IC/+20% dmg). Re-point any prereqs when removing nodes.
- **FIX:** Titan fires `weapon: 'tracer'` (a machine gun) — give the 1,300-HP colossus a real signature weapon.
- **PERF philosophy for the death-gun:** aura stacking is currently MULTIPLICATIVE (30 ammo depots ≈ 800× fire rate). Designer wants to *try the "super mega death gun"* — **DO NOT NERF YET.** When (if) it stops being fun, tame to ADDITIVE (`+25% +25%`, not `×125% ×125%`), never a hard cap. Radar-stacking already surfaced in-description.
- **AUDIT STILL TO DO:** strikes (`STRAT` table), enemy roster, achievements, raw UI strings — sweep for stale/redundant/weird, categorize KEEP/CUT/FIX/MERGE.
- **ON TRIAL** (suspected nothingburgers, watch in a real game before defending): Fire Control, Tractor Beam, Rail Hub.

---

## 🎨 BLOCKED ON THE DESIGNER
- **Capital Fabricator** super-ship art (designer is drawing the hull; transcribe to canvas once it exists).
- **Lore / story layer** (parents flagged the story as thin).
- **Music** — artist permissions for the default soundtrack.
- Possible spin-off: an **FPS in the same setting** (soldier's-eye view of the orbital siege; Helldivers-style co-op or extraction-under-bombardment).

## 🔒 DEFERRED DECISIONS (locked, apply later)
- Enemy-competence overhaul — **after 4.0** (make the enemy far smarter before touching the player further). Full design banked above in THE ENEMY OVERHAUL.
- Scenario menu rework — make the 4 intents feel like **equal choices**, not a difficulty ladder. Every cell of scenario×tier should be genuinely threatening (even Probe/Decap slam every capital hard in their short window).
- Difficulty as **×1 / ×2.5 / ×5 intensity tiers** decoupled from the 4 scenarios (the home for the brutal-vs-humane split). Escalation + planet-cracker only at top tiers of Invasion/Annihilation.
- Perf: it's **RENDERING**, not unit math, that's the bottleneck — optimize rendering (LOD/batching); web-workers/multicore can't touch Canvas2D (must stay on the main thread), so they're NOT the fix. Squad LOD shipped; general projectile/glow render cheapening still open (the old "9 FPS at 800 units" issue).
- **Strategic resources should grant BONUSES, not act as hard REQUIREMENTS** (the original design doc's intended fix — SRs ended up barely used vs. their intended role).
- **Graphical direction:** shape/outline reworks over tonal detail; the designer dislikes "jets become spaceships" at Space Age.
- **Redundancy consolidation audit (old list):** ground bombardment ×3, heavy tanks ×2, 1-item TECH tab; Comms-vs-Propaganda now RESOLVED (split roles); Scout CUT; Tractor Beam / Rail Hub on trial.
- **Musings banked:** optional ads + ~30 trivia pieces; a custom scenario editor already exists (`SCEN`).

---

## ✅ SHIPPED THIS SESSION (on `main`)
Emplacement-crumble fix · intro cinematic (plays every load, skip after first watch) · minimap (true world aspect, legible units) · camera auto-pull removed · **General** default · adaptive decapitation busters · persistent (killable) beachhead Shield Generator · super-units made proactive & multi-weapon (no HP inflation) · **Garrison economy v1** (weight budget, upkeep, HUD cell) · **infantry squads** · distinct **drone & warship textures** · **drone & fighter (with wingmen) squads** + grp:6 exploit fix · **Bombardment counterplay** (slow orbital buster descent, predictive interception, pinpoint damage) · **Stability overhaul** (drains→cap reductions, 0-stab = recoverable Capitulation Crisis not instant loss, stability reserve, demolition) · dispersed launcher buildings removed (TEL/Arsenal Ship/Ballistic Sub) · garrison orbital-lift exemption · mobile strike menu · demolition costs stability · buildings can slightly overlap · **Comms = reserve / Propaganda = cap** split + radar-stacking surfaced · **Particle Cannon → true hitscan** + **every projectile gun now LEADS** its shots (corrected intercept solve, 0%→100% hit rate on fast movers) · **squad perf** (4→3 bodies + render LOD, no phone-melt at scale) + **cleaner squad art** (staggered fireteam) + **water/drowning fix** (units never shoved into the sea; drowned units sink quietly, no explosion). Versions v3.20.1 → v3.31.0, each with a changelog entry.
